# Secrets / credentials - never commit these
access_token
kaggle.json
*.pem
.env
.env.local

# Python
backend/.venv/
__pycache__/
*.pyc

# Trained models (large binaries)
backend/model/*.keras
backend/model/*.h5
backend/model/saved_model/
backend/model/*.tflite
# ...but DO track the shipped demo model so the app works out of the box.
!backend/model/model.keras

# Node
frontend/node_modules/
frontend/dist/

# OS
.DS_Store
